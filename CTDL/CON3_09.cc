#include <bits/stdc++.h>

using namespace std;



int main()
{
    int t; cin >> t;
    while(t--)
    {
        int x, d, n;
        cin >> n;
        int check[n + 1] = {};
        vector <pair<int, int>> a(n + 1);
        for(int i = 1; i <= n; i++)
            cin >> x >> a[i].second >> a[i].first;
        sort(a.begin() + 1, a.end(), greater<pair<int, int>>());
        for(int i = )
        
    }
}   

